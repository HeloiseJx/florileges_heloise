---
title: "V2_maquette_florileges"
editor: visual

format:
  html:
    toc: true
    toc-location: left
    toc-title: "Sommaire"
    toc-depth: 3
    page-layout: full
    embed-resources: true

execute:
  echo: true
  message: false
  warning: false
  error: false
---

# IMPORT DE DONNEES & FORMATAGE

## librairies R

```{r librairies}
# R 4.4.2
library(cowplot) # 1.1.3
suppressWarnings(suppressMessages(library(dplyr))) # 1.1.4
suppressWarnings(suppressMessages(library(flexdashboard))) # 0.6.2
library(fmsb) # 0.7.6
library(ggplot2) # 3.5.1 
suppressWarnings(suppressMessages(library(here))) # 1.0.1
suppressWarnings(suppressMessages(library(httr))) # 1.4.7
library(jsonlite) # 1.8.9
suppressWarnings(suppressMessages(library(plotly))) # 4.10.4
suppressWarnings(suppressMessages(library(RMySQL))) # 0.10.29
library(stringr) # 1.5.1
library(tidyr)
library(lubridate)
library(sf)
library(vegan)
library(readxl)
library(patchwork)
```

## Jeux de données externes

Import des jeux de données externes permettant la constitution des référentiels.

-   Intercommunalités

-   Données biogéorégions

-   Données taxref

-   Données de couverture du sol OSO

-   Coordonnées plaine Nature

-   Coordonnées 1001 Voisins

-   Offre en nectar des plantes de la liste florilèges

### Intercommunalités

```{r couche publique des intercommunalites BD ADMIN EXPRESS}
# couche ADMIN EXPRESS - polygones 
invisible(capture.output({coord_epci <- st_read(here::here("maps", "ADMIN-EXPRESS_3-2__SHP_WGS84G_FRA_2024-12-18", "ADMIN-EXPRESS_3-2__SHP_WGS84G_FRA_2024-12-18", "ADMIN-EXPRESS", "1_DONNEES_LIVRAISON_2024-12-00243", "ADE_3-2_SHP_WGS84G_FRA-ED2024-12-18", "EPCI.shp"), quiet = TRUE) }))

# recuperation des coordonnees des intercommunalites d'interet
coord_plaineco = coord_epci %>% filter(NOM == "Plaine Commune") # emprise de Plaine Commune
coord_grand_paris = coord_epci %>% filter(NOM == "Métropole du Grand Paris") # emprise de la MGP
```

### Biogeoregions

biogeoregions : objet spatial détaillant les biogéorégions telles qu'elles sont définies par l'UE.

```{r couche publique des biogeoregions}
# biogeoregions
invisible(capture.output({biogeoregions = st_read(here::here("maps", "region_biogeo_fr", "region_biogeographique.shp")) %>%
  st_transform(crs = 4326) # passage du lambert 93 au WGS 84
}))

# certains sommets dupliqués : on corrige les erreurs
invalid_index <- which(!st_is_valid(biogeoregions))
biogeoregions[invalid_index, ] <- st_make_valid(biogeoregions[invalid_index, ])
```

### TaxRef

Import de la table TAXREF de patrinat, pour les plantes. on récupère les plantes (Regne = Plantae)

```{r}
ordre_taxonomique = read.csv2(here::here("data", "doc_heloise", "TAXREF_v18_2025", "TAXREF_Plantae.csv"))

```

### Données de couverture du sol = dt_protoc_oso

dt_protoc_oso permet d'avoir pour chaque session de la BD florileges les types de couverture du sol dans un rayon de 1km. La BD oso est un raster public distingant 23 catégories de couverture du sol, qu'il est possible d'agglomérer pour simplifier l'information.

J'agrège les couvertures du sols imperméables : im = urbain dense, urbain diffus, zones industrielles et commerciales, routes et je calcule pour chaque session_id un taux d'imperméabilisation (% de surface imperméabilisée) im (%) dans un rayon de 1 km.

![](data/oso_diagramme_couverture.png){fig-align="center" width="100%"}

***Table qui donne les informations de % d'imperméabilisation pour les sessions florileges :***

```{r child avec methode de calcul de la couverture du sol}
# knitr::knit_child("maquette_V2_child_couverture_sol_florileges.qmd", quiet = TRUE) #%>%
#   # knitr::asis_output()

```

```{r pour aller plus vite table de donnees oso existante enregistree en local pour florileges}
# paysage local ----
dt_protoc_oso = readRDS(here::here("data", "dt_florileges_oso_calcul_1000.rds")) %>%
  as.data.frame() %>%
  unique() %>%
  select(session_id, im) %>%
  unique()

```

### coord_plaine_nature = coordonnées Plaine Nature

coord_plaine_nature : Emprise du site Plaine Nature, aussi appelé Village des Athlètes ou VA.

```{r data florileges Plaine Nature}
# emprise spatiale Plaine Nature
json_plaine_nature = '{"type": "FeatureCollection","name": "villageAthletes","crs": { "type": "name", "properties": { "name": "urn:ogc:def:crs:OGC:1.3:CRS84" } },"features": [{ "type": "Feature", "properties": { }, "geometry": { "type": "MultiPolygon", "coordinates": [ [ [ [ 2.328302397552035, 48.922067901345237 ], [ 2.32981567138738, 48.923334320437128 ], [ 2.331231071790587, 48.924828257922385 ], [ 2.333060552098988, 48.92644581533628 ], [ 2.333745666123945, 48.926989936022409 ], [ 2.336877615952319, 48.925862115645039 ], [ 2.337186293699827, 48.926168806163972 ], [ 2.338014453510215, 48.925886848821236 ], [ 2.337291695857513, 48.925293249211663 ], [ 2.339234107049149, 48.924214858534825 ], [ 2.339776175288675, 48.923878475026783 ], [ 2.339023302733777, 48.923294745326601 ], [ 2.339994508329595, 48.922775319093915 ], [ 2.338887785673895, 48.922072848357288 ], [ 2.339836405093066, 48.921637509420734 ], [ 2.340626921275709, 48.922325145322304 ], [ 2.341515310890487, 48.921860126396467 ], [ 2.339746060386478, 48.920489779489607 ], [ 2.33878238351621, 48.919737804199627 ], [ 2.339490083717814, 48.919391495982325 ], [ 2.338661923907426, 48.917748972880204 ], [ 2.337961752431371, 48.916917796030702 ], [ 2.335770893296619, 48.917734130557733 ], [ 2.336331783350018, 48.918379767508057 ], [ 2.335240118145416, 48.918780503486708 ], [ 2.335371870842523, 48.918931397065208 ], [ 2.335940289621471, 48.918745872109326 ], [ 2.336663047274172, 48.91967348999794 ], [ 2.335443393735239, 48.920024743475018 ], [ 2.334521124855489, 48.919502809599713 ], [ 2.334411958335029, 48.919505283232859 ], [ 2.333275120777133, 48.919992586569272 ], [ 2.332469547143393, 48.920331469843077 ], [ 2.332160869395885, 48.920000007395508 ], [ 2.330542193402855, 48.920949864051636 ], [ 2.328121708138859, 48.921926911295728 ], [ 2.328302397552035, 48.922067901345237 ] ] ] ] } }]}'


coord_plaine_nature = st_read(
  dsn = json_plaine_nature,
  quiet = TRUE
) %>% 
  mutate(NOM = "plaine_nature")

```

### coord_1001_voisins = coordonnées 1001 voisins

coord_1001_voisins : Emprise du site 1001 Voisins, aussi appelé Village des Médias ou VM.

```{r data florileges 1001 voisins}
# emprise spatiale 1001 voisins
json_1001_voisins = '{"type": "FeatureCollection","name": "villageMedias","crs": { "type": "name", "properties": { "name": "urn:ogc:def:crs:OGC:1.3:CRS84" } },"features": [{ "type": "Feature", "properties": { }, "geometry": { "type": "MultiPolygon", "coordinates": [ [ [ [ 2.420072557139776, 48.943024583355111 ], [ 2.420073023091361, 48.943019380665795 ], [ 2.420067979421824, 48.943018487004629 ], [ 2.420063769199778, 48.943012880064778 ], [ 2.420062648382413, 48.943017542425913 ], [ 2.419853817681765, 48.942792045255153 ], [ 2.419349393069956, 48.942742595638627 ], [ 2.418965428066935, 48.942421171936957 ], [ 2.418878847723117, 48.942445896910577 ], [ 2.418167383158698, 48.941896999613682 ], [ 2.418240788232805, 48.9415916419726 ], [ 2.418090213721817, 48.941421036485124 ], [ 2.418216319874769, 48.941142874113687 ], [ 2.418348072571884, 48.941061279524 ], [ 2.41838571619963, 48.9406978110948 ], [ 2.419277870177235, 48.939142841017727 ], [ 2.419797352240144, 48.939268946538014 ], [ 2.420369535381899, 48.939120587069297 ], [ 2.420130075744921, 48.938710898976481 ], [ 2.421566602744255, 48.938299656704928 ], [ 2.421472493674887, 48.938156239773946 ], [ 2.421431085684365, 48.937970786235013 ], [ 2.421427321321591, 48.937931222724195 ], [ 2.421865869584844, 48.937875895574457 ], [ 2.422385116374816, 48.937819177454948 ], [ 2.421165462835882, 48.93592008779553 ], [ 2.420194257240064, 48.93620693409509 ], [ 2.418898466938361, 48.9344919564561 ], [ 2.418503449222627, 48.934288066621242 ], [ 2.418390587018131, 48.934288066621242 ], [ 2.417656982688909, 48.934573512156931 ], [ 2.417559203297922, 48.934700972633571 ], [ 2.418556759433161, 48.936048674271575 ], [ 2.417438037871099, 48.936412949305087 ], [ 2.41696466925218, 48.935812056353456 ], [ 2.416783979838995, 48.935767545476715 ], [ 2.415569972844152, 48.938260093454787 ], [ 2.418285960586102, 48.93892617368131 ], [ 2.418014691193634, 48.940914934347788 ], [ 2.417875645043659, 48.941176253680126 ], [ 2.417160416116465, 48.941482850134911 ], [ 2.418357483478821, 48.942489165584945 ], [ 2.419071862013785, 48.943142740749323 ], [ 2.416245675962213, 48.944625355205332 ], [ 2.414223150117127, 48.942834970265309 ], [ 2.412908305434752, 48.943906125930063 ], [ 2.414279581219374, 48.945333062294772 ], [ 2.414364227872746, 48.945388656392268 ], [ 2.414550450510164, 48.945414600283222 ], [ 2.414714100706683, 48.945470194289918 ], [ 2.414866464682752, 48.945614738417362 ], [ 2.414826962911178, 48.945718513430165 ], [ 2.414719743816907, 48.945840819418201 ], [ 2.414539164289713, 48.945911237881383 ], [ 2.414279581219374, 48.946615417047866 ], [ 2.413777344409368, 48.947252875934325 ], [ 2.413698340866222, 48.947430769984116 ], [ 2.413681411535547, 48.947534541220314 ], [ 2.414019998149034, 48.948134927707542 ], [ 2.414742316257807, 48.947986684802096 ], [ 2.416796408379629, 48.94761607561162 ], [ 2.416677903064908, 48.947226932999065 ], [ 2.416785122159179, 48.947152810252604 ], [ 2.416852839481876, 48.946934147509033 ], [ 2.416993917237496, 48.946615417047866 ], [ 2.417360719402107, 48.946341158974974 ], [ 2.421085172150464, 48.944373127777325 ], [ 2.420072557139776, 48.943024583355111 ] ] ] ] } }]}'


coord_1001_voisins = st_read(
  dsn = json_1001_voisins,
  quiet = TRUE
) %>% 
  mutate(NOM = "1001_voisins")

```

### Offre en nectar

```{r}
nectar = read_xlsx("C:/Git/florileges code gabriel/Nectar_Dispersion.xlsx") %>%
  mutate(taxon_nom_scientifique = str_extract(`Nom Espèce`, "^\\S+\\s+\\S+")) %>% 
  select(taxon_nom_scientifique, Nectar) %>%
  mutate(Nectar = as.numeric(Nectar))
```

## Import des données florilèges

### df_florileges = données florileges

Récupération de l'export à plat florilèges à partir des données mosaic :

```{r import des donnees florileges de la BD MOSAIC}

source(here::here("programs", "requete_florileges.R"))

DT::datatable(head(df_florileges))
```

### filtre et formatage sur les donnees brutes

codes Gabriel Sagot

-   Formatage des colonnes existantes

-   creation de nouvelles colonnes : date, abondance des taxons par session

```{r modification de la BD florileges}
df_florileges = df_florileges %>%
  # filtrage des sessions : on garde celles après 2010
  filter(session_date >= "2010-01-01") %>%
  
  # passage des colonnes taxon_count_ en format numérique
  mutate(across(starts_with("taxon_count_"), as.integer)) %>%
  
  # creation de nouvelles colonnes 
  mutate(session_date = as.Date(session_date),
         session_year = as.numeric(strftime(as.Date(session_date, "%Y-%m-%d"), "%Y")),
         session_month = as.numeric(strftime(as.Date(session_date, "%Y-%m-%d"), "%m")),
         session_day = as.numeric(strftime(as.Date(session_date, "%Y-%m-%d"), "%d")),

        # session_year_factor_all = factor(session_year, levels = as.character(min(session_year, na.rm = T):max(session_year, na.rm = T))),
         
         taxon_presence = rowSums(select(., starts_with("taxon_count_Q")), na.rm=TRUE), # compter le nb d'occurences des sp dans les quadrats
        
         taxon_presence_all = rowSums(select(., starts_with("taxon_count_")), na.rm=TRUE), # nb d'especes obtenu en considérant les sp dans et hors quadrat
        
         taxon_combine = if_else(is.na(taxon_nom_scientifique), taxon, taxon_nom_scientifique) # on recueille les infos que l'on a sur le nom des taxons : si taxon_nom_scientifique = NA alors taxon_combine = taxon, sinon taxon_combine = taxon_nom_scientifique
        ) %>%
  filter(taxon_combine != "null") # permet de filtrer les taxons sur lesquels on a 0 info quant à leur nom


DT::datatable(head(df_florileges))
```

### df_florileges_spatial = df florileges spatialise

Correction des géometries pour les sites (colonne site_geometry). A terme il faudrait appliquer les corrections de géométries de GSagot. Les site avec des géométries incorrectes sont d'abord corrigées et si cela ne fonctionne pas elles sont retirées.

```{r df florileges spatialise}
func_florileges_spatial_correct = function(dt_flori) {

  df_flori_spatial = dt_flori %>%
    # on enlève les NA
    filter(!is.na(site_geometry)) %>%
    
    unique() %>%
    # on converti en objet spatial la table
    mutate(geometry = st_as_sfc(site_geometry, crs = 4326)) %>% #projection WGS84
    select(-site_geometry) %>%
    st_as_sf() 
  
  # on modifie dans la mesure du possible les geometries invalides
  invalid_index <- which(!st_is_valid(df_flori_spatial))
  df_flori_spatial[invalid_index, ] <- st_make_valid(df_flori_spatial[invalid_index, ])
  
  # on supprime les autres
  df_flori_spatial_correct <- df_flori_spatial[st_is_valid(df_flori_spatial), ]
  
  return(df_flori_spatial_correct)
}

# donnes spatiales florileges
df_florileges_spatial = func_florileges_spatial_correct(df_florileges)

print(paste0("Nombre de sites perdus : ", as.character(n_distinct(unique(df_florileges$site_id)) - n_distinct(unique(df_florileges_spatial$site_id)))," sites"))



```

### df_florileges_centroid

Calcul d'un centroide par site florilèges.

```{r centroides des polygones florileges}
# un point par session florileges
df_florileges_centroid = st_centroid(df_florileges_spatial)

# library(leaflet)
# leaflet(data = df_florileges_centroid) %>% addTiles %>% addCircleMarkers()

# DT::datatable(head(df_florileges_centroid))

```

## df_florileges_VA = sites du Village des athlètes

Récupérations des sites et sessions pour le site du Village des athlètes.

N.B. : df_florileges_VA & df_florileges_VM sont des df temporaires pour VA et VM, ne contiennent pas les référentiels choisis pour les sessions, donc ces df ne peuvent pas servir au calcul des indicateurs nécessitant un référentiel. Utiliser à la place df_VA & df_VM. Simple récupération des sessions locales.

**Plaine Nature**

```{r jointure emprise VA et table florileges}
# df_florileges_VA = df_florileges_centroid %>%
#   st_join(coord_plaine_nature) %>%
#   filter(!is.na(NOM))


```

Comme il n'y a pas encore de site sur les sites SOLIDEO, ceux-ci viennent combler le manque pour faire un test.

```{r donnee de remplacement va}
df_florileges_VA = df_florileges %>% 
  filter(site_id == 1109) 

```

## df_florileges_VM = sites du Village des Medias

Récupérations des sites et sessions pour le site du Village des Medias

N.B. : df_florileges_VA & df_florileges_VM sont des df temporaires pour VA et VM, ne contiennent pas les référentiels choisis pour les sessions, donc ces df ne peuvent pas servir au calcul des indicateurs nécessitant un référentiel. Utiliser à la place df_VA & df_VM. Simple récupération des sessions locales.

**1001 voisins**

```{r jointure emprise VM et table florileges}
# df_florileges_VM = df_florileges_centroid %>%
#   st_join(coord_1001_voisins) %>%
#   filter(!is.na(NOM))
```

Comme il n'y a pas encore de site sur les sites SOLIDEO, ceux-ci viennent combler le manque pour faire un test.

```{r donnee de remplacement vm}
df_florileges_VM = df_florileges %>% 
  filter(site_id == 645) 
```

# CALCUL DU REFERENTIEL

Après avoir récupéré les données nécessaires, on calcule de façon automatisée le référentiel pour les sessions locales.

Le référentiel correspond à l'ensemble des sessions "comparables" aux sessions locales. Ce sont des sessions pour lesquelles il va faire sens de comparer les valeurs des indicateurs obtenues à l'échelle des sites solideo avec la valeur des indicateurs calculés pour ces sessions.

Pour construire ces référentiels il faut sélectionner les sessions adéquates. Cette sélection se base sur plusieurs critères dans le cas de florileges :

-   taux d'imperméabilisation : +- 15% de celui du site considéré (VA ou VM)

-   région écologique : biogéorégion

-   échelle administrative locale : Métropole du Grand Paris et pour les sessions réalisées sur le site du Village des Athlètes, l'emprise de Plaine Commune est considérée comme un référentiel potentiel

Ces critères sont classés par ordre d'impact sur les communautés de plantes observées.

Ainsi, l'ordre de priorité des critères est celui-ci :

**taux d'imperméabilisation** \> **région écologique** (biogéorégion) \> **région admninistrative** (MGP \> Plaineco)

En combinant ces critères on obtient plusieurs référentiels possibles et ils sont classés par ordre de priorité grâce à un score :

![](data/combi_criteres_ref_florileges.png){fig-align="center" width="70%"}

*tableau 1 : critères de formation des référentiels et combinaisons possibles, ainsi que leur score de priorité.*

## 1\_ table de données pour construire les référentiels

Différents jeux de données sont nécessaires au calcul des référentiels :

-   dt_ref_national : jeu de données florileges nettoyé, regroupe les sessions susceptibles de former un référentiel.

-   dt_florileges_biogeo : Sessions de l'ensemble du jeu de données florileges et les biogeorégions dans lesquelles elles se trouvent.

-   dt_florileges_MGP : Sessions de l'ensemble du jeu de données florileges qui se situent dans l'emprise de la métropole du Grand Paris.

-   dt_florileges_plaineco : Sessions de l'ensemble du jeu de données florileges qui se situent dans l'emprise de Plaine Commune.

### dt_ref_national = données nettoyées du jeu national

On nettoie les sessions :

-   filtre sur les sessions qui sont réalisées en dehors des périodes imposées par le protocole (15/05-31/07)

-   filtre sur les sessions "supplémentaires" : on garde une session par site

-   filtre sur les outliers de richesse en espèces par session et de frequence pour chaque taxon par session

-   je garde seulement les espèces de la liste florilèges

dt_ref_national = df clean contenant les sessions de référence pour le protocole florilèges.

**Nombre de sessions et dates**

```{r temporalite}
# CLEANING
# fonction cleaning rapide des BD pour avoir des données protocolées --> sessions en été et une seule session par site

#' Fonction qui vérifie les dates des sessions sur les sites et le nombre de sessions par an. vérifie par site et par an s'il y a plus de 1 session. Si c'est le cas, elle récupère la session réalisée le plus tôt. 
#'
#' @param df_flori contenant une colonne session_date, site_id, session_year, taxon_presence
#'
#' @returns dt4_clean le df avec 1 session par an par site entre juin et juillet
#' 
#'
#' 
func_dt_clean = function(df_flori) {
  
  # je filtre les sessions qui sont hors periode florilèges (juin & juillet)
dt1 = df_flori %>%
    filter(
    session_date >= ymd(paste0(year(session_date), "-05-15")),
    session_date <= ymd(paste0(year(session_date), "-07-31"))
  ) %>%
  # mutate(session_month = month(as.Date(session_date))) %>%
  # filter(session_date %in% c(5,6,7)) %>%
  # calcul du nombre de sessions par an par site
  group_by(site_id, session_year) %>%
  mutate(nb_session = n_distinct(session_id)) %>%
  ungroup()

# filtre sur les sessions "en trop" ; s'il y a des sites avec plusieurs sessions en période protocole, on ne garde que le premier passage

# on recupere les sites avec plus d'un passage par an
dt_avec_passages_en_trop = dt1 %>% filter(nb_session > 1)

if (length(dt_avec_passages_en_trop$session_id ) > 0 ) { # condition d'existence de sites avec sessions supplementaires
  
# enlever les sessions en trop pour n'avoir qu'une session par site
# je choisis de garder la session qui advient le plus tôt --> potentiellement encore une vegetation plus facile à identifier
dt2 = dt1 %>%
  filter(nb_session > 1) %>%
  group_by(session_year, site_id) %>%
  mutate(session_gardee = min(as.Date(session_date))) %>%
  ungroup() %>%
  filter(session_gardee == session_date) %>%
  select(-session_gardee) %>%

# cas où il y a une erreur de prise de notes cf site 1314 : 2 sessions le meme jour à la meme heure
# --> on garde la sessions avec le plus de taxons observés
  group_by(session_year, site_id, session_id) %>%
  mutate(nb_indiv = sum(taxon_presence)) %>%
  ungroup() %>%
  group_by(site_id, session_year)%>%
  filter(nb_indiv == max(nb_indiv)) %>%
  select(-nb_indiv) %>%
  ungroup()
  

# on cree le df contenant les sites où il y avait déjà une seule session
dt3 = dt1 %>%
  filter(nb_session == 1)

#le df final
# ajout des sites cleanes de leurs sessions supplementaires
dt4_clean = rbind(dt2, dt3)

} else {
  dt4_clean = dt1
}


return(dt4_clean)  

}


dt_clean_national = func_dt_clean(df_florileges) %>%
  select(-site_geometry)


```

**Création colonne présence-absence des sp**

```{r clean richesse et abondance}

# enlever les espèces hors liste
temp_dt_ref_national = dt_clean_national %>%
  filter(taxon_liste_florilege == 1) 

# creation colonne presence(1) absence(0) pour les calculs de richesse en especes
temp_dt_ref_national = temp_dt_ref_national %>%
  mutate(presence_sp = case_when(taxon_presence > 0 ~ 1, 
                                 TRUE ~ 0)) %>%
  group_by(session_id) %>%
  mutate(nb_sp_session = sum(presence_sp)) %>%
  ungroup()


```

**outliers richesse et frequence : exclusion des outliers**

Filtre sur les outliers de richesse en calculant le range interquartile et en appliquant un facteur k = 1.5 pour le calcul des seuils de outliers (on garde les données de richesse comprises dans l'intervalle \[ q\[1\] - k \* iqr ; q\[2\] + k \* iqr\], avec iqr = range interquartile, q\[1\] le premier quartile, q\[2\] le 3e quartile).

```{r}

# enlever les outlier pour la richesse 
k = 1.5
q <- stats::quantile(temp_dt_ref_national %>% select(session_id, nb_sp_session) %>% unique() %>% select(nb_sp_session), c(0.25, 0.75), na.rm = TRUE)
iqr <- q[2] - q[1]
    
lower_richesse = q[1] - k * iqr
upper_richesse = q[2] + k * iqr


# dt ref final
dt_ref_national = temp_dt_ref_national %>%
  filter(nb_sp_session >= lower_richesse & nb_sp_session <= upper_richesse ) %>%
  filter(taxon_presence >= 0 & taxon_presence <= 10) # 10 quadrats sur les sites


DT::datatable(head(dt_ref_national))


rm(temp_dt_ref_national)
rm(dt_clean_national)


# les sessions vides sont attendues, donc 0 est la valeur minimale de richesse et abondance, il n'y a pas d'outlier minimal


```

### df_florileges_plaineco = données Plaine Commune

Données florilèges dans l'emprise de Plaine Commune. Ces données peuvent servir de support pour construire un référentiel. Il n'y a pas d'équivalent d'emprise administrative "locale" pour le site 1001 Voisins, et je pars du principe que sur l'emprise de Dugny et du Bourget il n'y aura jamais de quoi constituer un référentiel étant donné la superficie restreinte.

```{r referentiel comprenant les données florileges dans Plaine Commune}

df_florileges_plaineco = df_florileges %>%

  # on transforme le df en objet spatial
  left_join(df_florileges_spatial) %>%
  st_as_sf() %>%

  # jointure spatiale avec les coordonnées de Plaine Commune
  st_join(coord_plaineco)  %>%
  filter(!is.na(NOM)) %>%

  # on repasse en df
  as.data.frame()  %>%

  # modif  pour avoir les memes colonnes que biogeoreg
  rename(N_DOMAINE = NOM) %>%
  mutate(CODE = "PC", DOMAINE_EU = 0)


```

### df_florileges_MGP = données Metropole Grand Paris

```{r data florileges Metropole Grand Paris}
df_florileges_MGP = df_florileges %>% 
  
  # on transforme le df en objet spatial
  left_join(df_florileges_spatial) %>%
  st_as_sf() %>%
  
  # jointure spatiale avec les coordonnées de Plaine Commune 
  st_join(coord_grand_paris)  %>%
  filter(!is.na(NOM)) %>%
  
  # on repasse en df
  as.data.frame()  %>%
  
  # modif  pour avoir les memes colonnes que biogeoreg
  rename(N_DOMAINE = NOM) %>%
  mutate(CODE = "GP", DOMAINE_EU = 0)

```

### df_florileges_biogeo = information sur les biogeoregions associées à chaque session

```{r df_florileges_biogeo}

df_florileges_biogeo = df_florileges %>%
  left_join(df_florileges_centroid) %>%
  st_as_sf() %>%
  # jointure avec biogeoregions 
  st_join(biogeoregions) %>%
  as.data.frame()
```

## 2\_ boucle de calcul

Les tables de données précédentes sont combinées pour former une super table qui donne par session l'information de :

-   biogéorégion

-   taux d'imperméabilisation (appartient au range de +- 15 % autour de la valeur im du site considéré ou non) : A partir de dt_protoc_oso on a l'information sur le taux d'imperméabilisation dans un buffer de 1km du site considéré et pour toutes les autres sessions du jeu de données florileges. A partir de ce df là on calcule pour chaque session si elle appartient à l'intervalle +- 15% du taux d'imperméabilisation du site considéré (cette information est indiquée dans la colonne paysage_local).

-   une échelle administrative : la métropole du grand paris ou Plaine Commune pour le site du village des athlètes

Une boucle de calcul permet de combiner ces critères pour former les référentiels **potentiels**.

```{r referentiels potentiels}

# creation de tous les df referentiels potentiels
# fonction qui considère qu'on connaît déjà le taux d'urbanisation qui nous interesse et la biogeoregion associée à notre site.
# fonction qui calcule pour chaque référentiel les sessions qui constituent ce référentiel. Elle réalise ensuite un bind de ces sessions


func_creation_combi_critere = function(df_florileges_site, PC = FALSE) {
  

taux_urba = df_florileges_site %>%
  select(session_id, site_id) %>%
  unique() %>%
  left_join(dt_protoc_oso) %>%
  pull(im) %>%
  unique()

biogeo = df_florileges_site %>%
  left_join(df_florileges_biogeo %>% select(session_id, N_DOMAINE)) %>%
  pull(N_DOMAINE) %>%
  unique()


# no_ref ----
  dt_ref_noref = df_florileges %>%
  
  # jointure avec le paysage local
  left_join(dt_protoc_oso) %>%
  select(site_id, site, session_id, im, taxon, taxon_presence, taxon_liste_florilege, session_year) %>%
   # select(-geometry) %>%
  unique() %>%
  
  mutate(referentiel = "4no_ref") %>%
  # mutate(nb_session = n_distinct(session_id)) %>%
  # mutate(nb_site = n_distinct(site_id)) %>%
  # select(nb_session, nb_site, referentiel) %>%
  unique()

  
  
# u15_VA ----
dt_ref_u15 = 
  df_florileges %>%
    
  # jointure avec le paysage local
  left_join(dt_protoc_oso)  %>%
   # select(-geometry) %>%
  unique() %>%
  
  # selection de colonnes
  select(site_id, site, session_id, im, taxon, taxon_presence, taxon_liste_florilege, session_year) %>%

  # selection sites dont l'urbanisation est comprise entre 65% et 95%
  filter(im <= taux_urba + 0.15 & im >= taux_urba - 0.15) %>%
  mutate(referentiel = "3u15") #%>%
  # mutate(nb_session = n_distinct(session_id)) %>%
  # mutate(nb_site = n_distinct(site_id))



# u15_MGP ----
dt_ref_u15_MGP = df_florileges %>%
  left_join(df_florileges_MGP) %>%
  filter(!is.na(N_DOMAINE)) %>%
    
  # jointure avec le paysage local
  left_join(dt_protoc_oso)  %>%
  unique() %>%
  
  select(site_id, site, session_id, im, taxon, taxon_presence, taxon_liste_florilege, session_year) %>%
    
  # selection sites dont l'urbanisation est comprise entre 65% et 95%
  filter(im <= taux_urba + 0.15 & im >= taux_urba - 0.15) %>%
  mutate(referentiel = "1u15_MGP")# %>%
  # mutate(nb_session = n_distinct(session_id)) %>%
  # mutate(nb_site = n_distinct(site_id))
  # 


# Pour le village des athlètes, on considère l'échelle administratives Plaine Commune comme un référentiel potentiel
if (PC == TRUE) {
  # u15_plaineco ----
  dt_ref_u15_plaineco = df_florileges %>%
    left_join(df_florileges_plaineco) %>%
    # filter(!is.na(N_DOMAINE)) %>%
    
    # jointure avec le paysage local
    left_join(dt_protoc_oso)  %>%
    unique() %>% 
  
    select(site_id, site, session_id, im, taxon, taxon_presence, taxon_liste_florilege, session_year) %>%
    
    # selection sites dont l'urbanisation est comprise entre 65% et 95%
    filter(im <= taux_urba + 0.15 & im >= taux_urba - 0.15) %>%
    mutate(referentiel = "0u15_plaineco")# %>%
  # mutate(nb_session = n_distinct(session_id)) %>%
  # mutate(nb_site = n_distinct(site_id))
  #
  
}



# u15_biogeo ----
dt_ref_u15_biogeo = 
      df_florileges_biogeo %>%

  # jointure avec le paysage local
  left_join(dt_protoc_oso)  %>%
  # select(-geometry) %>%
  unique() %>%
  
  # selection des sites de la biogeoregion
  filter(N_DOMAINE == biogeo) %>%
  
  # sessions clean
  select(site_id, site, session_id, im, taxon, taxon_presence, taxon_liste_florilege, session_year) %>%

  # selection sites dont l'urbanisation est comprise entre 65% et 95%
  filter(im <= taux_urba + 0.15 & im >= taux_urba - 0.15)%>%
  mutate(referentiel = "2u15_biogeo")#%>%
  # mutate(nb_session = n_distinct(session_id)) %>%
  # mutate(nb_site = n_distinct(site_id))




if (PC == TRUE) {
  bind_df = dt_ref_u15_biogeo %>%
    bind_rows(dt_ref_u15_MGP) %>%
    bind_rows(dt_ref_u15_plaineco) %>%
    bind_rows(dt_ref_u15) %>%
    bind_rows(dt_ref_noref) %>%
    unique() %>%
    
    # extraction critere priorite
    mutate(
      priorite = str_extract(referentiel, "\\d+"),
      referentiel = str_remove(referentiel, "\\d+")
    )
  
} else {
  bind_df = dt_ref_u15_biogeo %>%
    bind_rows(dt_ref_u15_MGP) %>%
    bind_rows(dt_ref_u15) %>%
    bind_rows(dt_ref_noref) %>%
    unique() %>%
    
    # extraction critere priorite
    mutate(
      priorite = str_extract(referentiel, "\\d+"),
      referentiel = str_remove(referentiel, "\\d+")
    )
}


return(bind_df)

}


# avec toutes les données pour pouvoir associer toutes les sessions à un referentiel
bind_VA_all = func_creation_combi_critere(df_florileges_site = df_florileges_VA, PC = TRUE)
bind_VM_all = func_creation_combi_critere(df_florileges_site = df_florileges_VM)


DT::datatable(head(bind_VA_all %>% select(-starts_with("taxon")) %>% unique() %>% arrange(session_id)))

```

## 3_conditions et sélection

On cherche à sélectionner un référentiel par session locale parmi tous les référentiels qui leur sont liés. La sélection des référentiels se base sur 3 critères :

-   il faut que le poids des sessions locales rattachées à ce référentiel n'excède pas 20 % (nb de sessions locales rattachées à ce ref / nb de sessions constituant le référentiel \< 0.2)

-   La taille du référentiel doit être supérieure ou égale à 50 sites

-   Tenant compte des deux critères précédent, le score de finesse du référentiel doit être minimal.

Ainsi, pour un site avec plusieurs sessions, on se retrouve potentiellement avec plusieurs référentiels.

Le tableau montre les sessions réalisées sur le site, les référentiels auxquelles elles sont comparées et il reste les colonnes des 3 critères de décision (n = le nombre de sessions constituant le référentiel, finesse = score de finesse du référentiel et poids = le poids des données locales rattachées au référentiel dans le référentiel considéré).

A l'issue de cette selection on a les tables contenant les données produites sur les sites (df_VA et df_VM) et les tables contenant les sessions de référence pour le site considéré (dt_ref_VA et dt_ref_VM).

```{r critere 1 : taille referentiel et priorite}
func_sel_referentiel = function(df_florileges_site, bind_all_site) {
  
  # df calculant la taille de tous les referentiels possibles pour le site
  dt_taille_ref = bind_all_site %>%
  
  # on filtre les sessions de propres" 
  filter(session_id %in% unique(dt_ref_national$session_id))  %>%
  
  # calcul taille des referentiels
  group_by(referentiel) %>%
  mutate(nb_session = n_distinct(session_id)) %>%
  mutate(nb_site = n_distinct(site_id)) %>%
  ungroup() %>%
  select(session_id, site_id, priorite, referentiel, nb_session, nb_site) %>% 
  unique()

  
# calcul du poids des données locales dans le referentiel
dt_ref = bind_all_site %>%
  filter(site_id %in% unique(df_florileges_site$site_id)) %>%
  left_join(dt_taille_ref %>% select(-session_id, -site_id)) %>%
  unique() %>%
  
  # calcul poids données locales dans le referentiel
  # group_by(referentiel) %>%
  mutate(poids = n_distinct(site_id) / nb_site) %>%
  ungroup() %>%
  
  
  # selection
  filter(poids < 0.2) %>%
  filter(nb_site > 49) %>%
  group_by(site_id) %>%
  filter(priorite == min(priorite)) %>%
  ungroup() %>%
  unique() %>%
  select(session_id, referentiel) %>% 
  unique() %>%
  left_join(df_florileges_site)
  
  return(dt_ref)
  
}

# par site et sessions locales (VA et VM) : calcul des référentiels à choisir pour chaque site
df_VA = func_sel_referentiel(df_florileges_site = df_florileges_VA, bind_all_site = bind_VA_all)

df_VM = func_sel_referentiel(df_florileges_site = df_florileges_VM, bind_all_site = bind_VM_all) 

DT::datatable(head(df_VA))
```

### dt_ref_VA et dt_ref_VM

```{r dt de reference pour les sites}
# récupération de toutes les sessions constitutives de ces référentiels
dt_ref_VA = bind_VA_all %>% 
  filter(referentiel %in% unique(df_VA$referentiel)) %>%
  filter(session_id %in% unique(dt_ref_national$session_id)) %>%
  left_join(dt_ref_national)  %>% 
  filter(!is.na(nb_sp_session))
  

dt_ref_VM = bind_VM_all %>%
  filter(referentiel %in% unique(df_VM$referentiel)) %>%
  filter(session_id %in% unique(dt_ref_national$session_id)) %>%
  left_join(dt_ref_national) %>% 
  filter(!is.na(nb_sp_session))
  
DT::datatable(head(dt_ref_VA))


```

### df_VA et df_VM

```{r ajout colonne richesse par session pour les sessions locales}

df_VA = df_VA %>%
  mutate(presence_sp = case_when(taxon_presence > 0 ~ 1, 
                                 TRUE ~ 0)) %>%
  group_by(session_id) %>%
  mutate(nb_sp_session = sum(presence_sp)) %>%
  ungroup()

df_VM = df_VM %>%
  mutate(presence_sp = case_when(taxon_presence > 0 ~ 1, 
                                 TRUE ~ 0)) %>%
  group_by(session_id) %>%
  mutate(nb_sp_session = sum(presence_sp)) %>%
  ungroup()

DT::datatable(df_VA)

```

# INDICATEURS - V2

## Occurence des espèces

Ces fréquences correspondent à la part de sessions pour lesquelles le taxon est observé, i.e. la probabilité d'observer le taxon.

```{r}
func_freq_sp = function(dt_flori) {
  
  df_freq = dt_flori %>%
    filter(taxon_liste_florilege == 1) %>%
    select(session_id, taxon, taxon_nom_scientifique, presence_sp) %>%
    group_by(taxon) %>%
    mutate(nb_session_present = sum(presence_sp)) %>%
    ungroup() %>%
    mutate(freq_taxon = 100 * nb_session_present / n_distinct(session_id)) %>%
    filter(freq_taxon != 0) %>%
    select(-session_id, -presence_sp) %>%
    unique()
  
  return(df_freq)
}
```

```{r}
# je modifie les tables pour avoir le même nombre de lignes et le même ordre dans l'affichage


dt_freq_site_temp = func_freq_sp(dt_flori = df_VM) %>%
  arrange(desc(freq_taxon)) %>%
  mutate(ordre = row_number())

dt_freq_ref = func_freq_sp(dt_flori = dt_ref_VM) %>%
  left_join(dt_freq_site_temp %>% select(taxon, ordre)) %>%
  arrange(desc(freq_taxon)) %>%
  mutate(ordre = case_when(is.na(ordre) ~ row_number() + length(unique(dt_freq_site_temp$taxon)),
                           TRUE ~ ordre))

dt_freq_site = dt_freq_site_temp %>%
  bind_rows(dt_freq_ref %>% select(taxon, ordre) %>% filter(! taxon %in% dt_freq_site_temp$taxon)) %>%
  mutate(freq_taxon = replace_na(freq_taxon, 0))

# Graphiques
plot_site = dt_freq_site %>%
  ggplot(aes(x = reorder(taxon, - ordre), y = freq_taxon)) +
  geom_bar(stat = "identity", position = "dodge", width = 0.6, fill = "#F76E5EFF") +
  coord_flip() +
  theme_minimal() +
  ylab("% de sessions pour lesquelles l'espèce est présente") +
  xlab("espèces")+
    scale_y_continuous(limits = c(0,  100)) +
      theme(axis.text.y = element_text(size = 6)) +
  theme(legend.position = "none") +
  labs(title = "site") 

plot_ref = dt_freq_ref %>%
  ggplot(aes(x = reorder(taxon, -ordre), y = freq_taxon)) +
  geom_bar(stat = "identity", position = "dodge", width = 0.6, fill = "#11C2B5FF") +
  coord_flip() +
  theme_minimal() +
    scale_y_continuous(limits = c(0,  100)) +
    theme(axis.text.y = element_text(size = 6)) +
    theme(legend.position = "none") +
    labs(title = "référence", 
         y = "", x = "")

plot_combi = plot_site + plot_ref
plot_combi
```

## score d'offre en nectar du site : diagramme radar V2

On cherche à faire une jauge allant de 0 à 100% décrivant les sites du référentiels ordonnés par valeur d'offre en nectar. On place ensuite les sessions réalisées localement sur cette jauge, en synthétisant les offres en nectar obtenues sous forme de médiane.

**Positionnement de l'a richesse'offre en nectar du site par rapport au référentiel auquel sont liées les sessions du site :** La colonne "norm" correspond au pourcentage de sessions du référentiel qui ont des valeurs d'offre en nectar inférieures à la valeur du site. Dans cette fonction, on cherche à positionner la valeur médiane d'activité du site, par rapport aux sessions du référentiels. On cherche à obtenir un pourcentage qui nous dit : pour la médiane d'offre en nectar du référentiel X calculée à partir des sessions locales, Y % des sessions du référentiels ont des indices d'offre en nectar inférieurs.

Il peut y avoir plusieurs points pour un même site local considéré, dans le cas où celui-ci comprend plusieurs sites réalisés dans des habitats différents.

### calcul et graphe

Calcul du score d'offre en nectar sur le site par référentiel.

```{r calcul score riches}
fun_nectar_score = function(dt_site, dt_ref) {
  
  
  # calcul quantité nectar moyen sur le site
  # = sum(freq plante i  *  nectar i) / nb_tot_plantes sur le site
  dt_nectar_local = dt_site %>%
    filter(taxon_liste_florilege == 1) %>%
    select(session_id, taxon, taxon_nom_scientifique, taxon_presence) %>%
    left_join(nectar) %>%
    na.omit() %>% # on retire les taxons pour lesquels on a pas l'info de la quantité de nectar
    
    # calcul de freq plante i
    group_by(taxon) %>%
    mutate(freq = sum(taxon_presence)) %>%
    ungroup() %>%
    filter(freq != 0) %>%
    mutate(freq_taxon  = freq / (n_distinct(session_id) * 10)) %>%
    select(-session_id, -taxon_presence) %>%
    unique() %>%
    
    # calcul apport en nectar par plante sur le site
    mutate(nectar_i = freq_taxon * Nectar) %>%
    
    # calcul score de nectar du site
    mutate(nectar_site =  sum(nectar_i) / n_distinct(taxon)) %>%
    
    select(nectar_site) %>%
    unique()
    
  
  
     # calcul quantité nectar moyen par session
  # = sum(freq plante i  *  nectar i) / nb_tot_plantes dans la session
  dt_nectar_ref = dt_ref %>%
    filter(taxon_liste_florilege == 1) %>%
    select(session_id, taxon, taxon_nom_scientifique, taxon_presence) %>%
    left_join(nectar) %>%    
    na.omit() %>% # on retire les taxons pour lesquels on a pas l'info de la quantité de nectar
    
    # calcul de freq plante i
    group_by(taxon, session_id) %>%
    mutate(freq_taxon = sum(taxon_presence) /10) %>%
    filter(freq_taxon != 0) %>%
    ungroup() %>%
    unique() %>%
    
    # calcul apport en nectar par plante sur le site
    mutate(nectar_i = freq_taxon * Nectar) %>%
    
    # calcul score de nectar de la session
    group_by(session_id) %>%
    mutate(nectar_session =  sum(nectar_i) / n_distinct(taxon)) %>%
    ungroup() %>%
    select(session_id, nectar_session) %>%
    unique()
    
  
  
  # récupération de la position du score en nectar du site en pourcentage de sessions : pourcentage de sessions du référentiel qui ont un indice d'activité inférieur à celui de la communauté observée sur le site
  dt_ref_rang = dt_nectar_ref %>%
    arrange(nectar_session) %>%
    # numérotation des sessions
    mutate(num = order(nectar_session) - 1,
           # -1 pour partir de 0
           # normaliser entre 0 et 100
           norm = 100 * num / max(num),
           nb_session = n_distinct(session_id)) %>%
    # gestion des sessions ayant la même activité --> le même pourcentage
    group_by(nectar_session) %>%
    mutate(norm = max(norm))%>%
    ungroup()
  
  
  # je positionne les médianes sur l'échelle de 0 à 100 de l'activité des sessions 
  temp = dt_ref_rang %>%
    filter(nectar_session <= unique(dt_nectar_local$nectar_site)) %>%
    filter(norm == max(norm)) %>%
    select(
      norm,
      nectar_session
    ) %>%
    mutate(nb_an = n_distinct(dt_site$session_year)) %>%
    unique()
  
  
  return(temp)
  
  
}

dt_activite_VM = fun_nectar_score(dt_site = df_VM, dt_ref = dt_ref_VM)
dt_activite_VA = fun_nectar_score(dt_site = df_VA, dt_ref = dt_ref_VA)


print(dt_activite_VM)
```

**Graphe**

Une fois que toutes les valeurs d'activité sont échelonnées entre 0 et 100, on affiche sur une même jauge les points des médianes pour le référentiel local --\>1 représentation

```{r representation graphique score riches}
#| column: screen-inset-shaded
#| layout-nrow: 1

fun_graphe_nectar_gradient = function(dt_ref, dt_local, site) {
  
  temp = fun_nectar_score(dt_ref = dt_ref, dt_site = dt_local)
  
  
  # GRAPHE
  #tracer une ligne de 0 à 100 qui change de couleur (gradient orange vert)
  # création du dégradé
  n_steps <- 100  # finesse du dégradé
  
  temp_grad <- temp |>
    rowwise() |> # travaille ligne par ligne
    mutate(y_seq = list(seq(0, 100, length.out = n_steps))) |>
    unnest(y_seq) |>
    mutate(yend = lead(y_seq), value = y_seq / max(temp$norm)) |>
    filter(!is.na(yend))
  
  
  # placer la médiane et l'écart type sur un graphe pour les sessions locales
    plot = 
    ggplot(temp_grad) +
    geom_segment(aes(
      x = "jauge de nectar",
      xend = "jauge de nectar",
      y = y_seq,
      yend = yend,
      color = value
    ),
            size = 4,
      guide = "none") +
      scale_color_gradient(low = "red", high = "green", guide = "none") +
    geom_point( aes(x = "jauge de nectar", y = norm), size = 4) +
    scale_fill_brewer(palette = "Set2") +
    # geom_text(data = temp_grad, aes(
    #   x = combi,
    #   y = 110#,
      # label = paste0("n = ", nb_session, " sessions \n dans le référentiel")
    # )) +
    labs(y = "pourcentage de sessions du référentiel", title = paste0("score d'offre en nectar obtenue sur le site ", site), x = "") +
    theme_minimal() +
    # theme(legend.position = "none") 
     geom_text( aes(x = "nombre d'année(s) de suivi\n locales", y = norm, label = paste0(nb_an, " année(s)")))
  
  return(plot)
  
}

fun_graphe_nectar_gradient(dt_ref = dt_ref_VM, dt_local = df_VM, site = "VM")
fun_graphe_nectar_gradient(dt_ref = dt_ref_VA, dt_local = df_VA, site = "VA")


```

# INDICATEURS - V3

## typicité

Calcul de la typicité pour chaque site : pourcentage relatif de plantes typiques des friches, prairies et gazon en nombre de quadrats sur lesquels elles sont présentes sur toutes les années de suivi = part que représente chaque habitat dans le relevé floristique.

Ces pourcentages sont calculés en tenant compte du biais inhérent à la liste florilèges qui contient une majorité de plantes prairiales et moins d'espèces de friches et gazons.(prairie = 32/60, gazon = 11/60, friche = 17/60 espèces)
Ainsi il s'agit des pourcentages réels de quadrats étudiés sur lesquels on observe l'habitat, pondéré par la part de l'habitat dans la liste d'espèces.


Cette fonction calcule ces pourcentages pour un df donné.

```{r}

# Import de la table des traits des espèces.
# On conserve uniquement les espèces présentes dans la Liste Florilège
# (ListeFlorilege == 1), qui sert ici de référence.
dt_traits = read.csv2("data/SpeciesTrait2024.csv") %>% 
  filter(ListeFlorilege == 1)


# Nombre d'espèces de référence par type d'habitat.
# Ces valeurs peuvent notamment servir de référence pour interpréter
# la représentativité des espèces caractéristiques de chaque habitat.
n_especes <- c(
  friche = 17,
  gazon = 11,
  prairie = 32
)
```



```{r}


fun_typicite_calcul = function(table_flori, site) {

  # ---------------------------------------------------------------------------
  # Préparation des données
  # - sélection des variables utiles
  # - harmonisation de certains noms scientifiques
  # - ajout de l'habitat de chaque espèce via la table des traits
  # - suppression des espèces sans habitat renseigné
  # ---------------------------------------------------------------------------
  dt_fonctionnalite = table_flori %>%
    select(session_id, site_id, session_year, taxon,
           taxon_nom_scientifique, taxon_presence, presence_sp) %>%
    unique() %>%
    mutate(
      taxon_nom_scientifique = case_when(
        taxon_nom_scientifique == "Silene latifolia subsp. alba" ~ "Silene latifolia",
        taxon_nom_scientifique == "Cerastium fontanum subsp. vulgare" ~ "Cerastium fontanum",
        taxon_nom_scientifique == "Plantago major subsp. major" ~ "Plantago major",
        taxon_nom_scientifique == "Medicago sativa subsp. sativa" ~ "Medicago sativa",
        taxon_nom_scientifique == "Taraxacum sectionruderalia" ~ "Taraxacum section ruderalia",
        # taxon_nom_scientifique == "Festuca rubra Gr." ~ "Festuca rubra",
        TRUE ~ taxon_nom_scientifique
      )
    ) %>%
    left_join(dt_traits %>% rename(taxon_nom_scientifique = Plant)) %>%
    filter(!is.na(HABITAT))
  
  
  # ---------------------------------------------------------------------------
  # Calcul de la proportion (%) de plantes appartenant à chaque habitat
  # pour chaque année du site étudié
  # ---------------------------------------------------------------------------
  dt_typicite_temp = dt_fonctionnalite %>%
    
    # Calcul du nombre total de plantes observées par année
    group_by(site_id) %>%
    
    # Somme des fréquences de présence de toutes les espèces
    # observées sur le site.
    mutate(sum_freq = sum(taxon_presence)) %>%
    ungroup() %>%
  
  # proportion d'habitat en intégrant une pondération des occurences par le nombre d'espèces propre à chaque habitat de la liste florilèges
  # pondération par le % d'espèces de la liste correspondant à chaque habitat :
  # prairie = 32/60, gazon = 11/60, friche = 17/60
  group_by(site_id, HABITAT) %>%
    summarise(abondance = sum(taxon_presence, na.rm = TRUE),
              .groups = "drop") %>%
    mutate(n_especes = n_especes[HABITAT], score = abondance / n_especes) %>%
    # normalisation des scores
    group_by(site_id) %>%
    mutate(prop_habitat = 100 * score / sum(score)) %>%
    ungroup()
  

  
  
  # ----------------------------------------------------------
  # Création de toutes les combinaisons site × habitat
  # ----------------------------------------------------------
  
  # Liste des habitats présents dans la table de référence.
  dt_habitat = dt_traits %>%
    select(HABITAT) %>%
    unique()
  
  # Liste des sites pour lesquels une typicité a été calculée.
  dt_sites = dt_typicite_temp %>%
    select(site_id) %>%
    unique() %>%
    
    # Création de toutes les combinaisons possibles entre les sites
    # et les habitats.
    #
    # Cette étape est importante car un site peut ne contenir aucune
    # espèce typique d'un habitat donné. Dans ce cas, la combinaison
    # site × habitat n'existerait pas dans dt_typicite_temp.
    crossing(dt_habitat)
  
  
  # ----------------------------------------------------------
  # Création de la table finale de typicité
  # ----------------------------------------------------------
  
  # On rattache les proportions calculées à toutes les combinaisons
  # site × habitat.
  #
  # Si aucune espèce d'un habitat n'est présente sur un site,
  # le left_join() produit une valeur NA. Celle-ci est remplacée
  # par 0 afin d'obtenir une valeur explicite de 0 %.
  dt_typicite = dt_sites %>%
    left_join(dt_typicite_temp) %>%
    mutate(
      prop_habitat = replace_na(prop_habitat, 0)
    )
  

  
  return(dt_typicite)
  
}

dt_typicite_VA = fun_typicite_calcul(table_flori = df_VA)
DT::datatable(head(dt_typicite_VA))

dt_typicite_refVA = fun_typicite_calcul(dt_ref_VA)


```





**graphe typicite**

Représentation en densité des typicités des sites de référence. Affichage en point des valeurs de typicité pour les sites considérés localement --\> fonctionne pour un seul site en local.



### typicité moyenne du site

```{r}

fun_plot_typicite = function(dt_ref, dt_site) {
  
  dt_typicite_ref = fun_typicite_calcul(dt_ref)
  dt_typicite_site = fun_typicite_calcul(dt_site)
  
  plot =
  ggplot() +
    geom_density(
      data = dt_typicite_ref,
      aes(x = prop_habitat, group = HABITAT, fill = HABITAT),
      color = "#11C2B5FF"
    ) +
    geom_point(
      data = dt_typicite_site,
      aes(x = prop_habitat, y = 0, group = HABITAT),
      color = "#F76E5EFF",
      size = 5
    ) +
    facet_wrap( ~ HABITAT) +
    theme(
      legend.position = "none",
      panel.spacing = unit(0.3, "lines"),
      axis.ticks.x = element_blank()
    ) +
    scale_fill_manual(values = c(
      "prairie" = "#11C2B5FF",
      "friche" = "#11C2B5FF",
      "gazon" = "#11C2B5FF"
    )) +
    labs(y = "densité", x = "proportion relative d'espèces de l'habitat")
  
  
  return(plot)
  
  
}

fun_plot_typicite(dt_ref = dt_ref_VA, dt_site = df_VA)

fun_plot_typicite(dt_ref = dt_ref_VM, dt_site = df_VM)


```

### suivi de la typicité en fonction des années pour un site

calcul pour un site donné de la typicité par année (abondance moyenne par espèce de plante, par habitat). Cet indice permet de corriger le biais en faveur des espèces prairiales de la liste florilèges (sur 60 espèces : 32 sont des espèces de prairies, 17 des espèces de friche, 11 des espèces de gazon)

prairie = 32/60, gazon = 11/60, friche = 17/60 espèces

```{r}


fun_evol_typicite = function(table_flori, site) {

  # ---------------------------------------------------------------------------
  # Préparation des données
  # - sélection des variables utiles
  # - harmonisation de certains noms scientifiques
  # - ajout de l'habitat de chaque espèce via la table des traits
  # - suppression des espèces sans habitat renseigné
  # ---------------------------------------------------------------------------
  dt_fonctionnalite = table_flori %>%
    select(session_id, site_id, session_year, taxon,
           taxon_nom_scientifique, taxon_presence, presence_sp) %>%
    unique() %>%
    mutate(
      taxon_nom_scientifique = case_when(
        taxon_nom_scientifique == "Silene latifolia subsp. alba" ~ "Silene latifolia",
        taxon_nom_scientifique == "Cerastium fontanum subsp. vulgare" ~ "Cerastium fontanum",
        taxon_nom_scientifique == "Plantago major subsp. major" ~ "Plantago major",
        taxon_nom_scientifique == "Medicago sativa subsp. sativa" ~ "Medicago sativa",
        taxon_nom_scientifique == "Taraxacum sectionruderalia" ~ "Taraxacum section ruderalia",
        # taxon_nom_scientifique == "Festuca rubra Gr." ~ "Festuca rubra",
        TRUE ~ taxon_nom_scientifique
      )
    ) %>%
    left_join(dt_traits %>% rename(taxon_nom_scientifique = Plant)) %>%
    filter(!is.na(HABITAT))
  
  
  # ---------------------------------------------------------------------------
  # Calcul du nombre d'années de suivi par site
  # (information conservée mais non utilisée dans la suite de la fonction)
  # ---------------------------------------------------------------------------
  dt_nb_an = dt_fonctionnalite %>%
    group_by(site_id) %>%
    mutate(nb_an = n_distinct(session_year)) %>%
    ungroup()
  
  
  # ---------------------------------------------------------------------------
  # Calcul de la proportion (%) de plantes appartenant à chaque habitat
  # pour chaque année du site étudié
  # ---------------------------------------------------------------------------
  dt_typicite_annuelle_temp = dt_nb_an %>%
    filter(site_id == site) %>%                     # conserver uniquement le site étudié
    
    # Calcul du nombre total de plantes observées par année
    group_by(session_year) %>%
    mutate(sum_freq = sum(taxon_presence)) %>%
    ungroup() %>%
  
  # proportion d'habitat en intégrant une pondération des occurences par le nombre d'espèces propre à chaque habitat de la liste florilèges
  # pondération par le % d'espèces de la liste correspondant à chaque habitat :
  # prairie = 32/60, gazon = 11/60, friche = 17/60
  group_by(site_id, HABITAT, session_year) %>%
    summarise(abondance = sum(taxon_presence, na.rm = TRUE),
              .groups = "drop") %>%
    mutate(n_especes = n_especes[HABITAT], score = abondance / n_especes) %>%
    # normalisation des scores
    group_by(site_id, session_year) %>%
    mutate(prop_habitat = 100 * score / sum(score)) %>%
    ungroup()
  

  
  
  # ---------------------------------------------------------------------------
  # Compléter les données pour obtenir toutes les combinaisons
  # année × habitat, même lorsqu'un habitat est absent une année donnée
  # ---------------------------------------------------------------------------
  
  # Liste des habitats présents dans la base de traits
  dt_habitat = dt_traits %>%
    select(HABITAT) %>%
    unique()
  
  # Liste des années disponibles pour le site
  dt_sessions = dt_typicite_annuelle_temp %>%
    select(session_year) %>%
    unique() %>%
    crossing(dt_habitat)
  
  # Table finale contenant, pour chaque année,
  # un pourcentage pour chacun des habitats
  dt_typicite_annuelle = dt_sessions %>%
    left_join(dt_typicite_annuelle_temp) %>%
    mutate(
      prop_habitat = replace_na(prop_habitat, 0)
    ) %>%
    select(-score)
  

  
  return(dt_typicite_annuelle)
  
}

dt_evol_typicite_VA = fun_evol_typicite(table_flori = df_VA, site = 1109)
DT::datatable(head(dt_evol_typicite_VA))

dt_evol_typicite_refVA = fun_evol_typicite(dt_ref_VA, site = 1)


```

**représentation graphique de l'évolution de la typicité**

affichage pour un site donné de l'évolution des parts relatives des différents habitats dans le relevé floristique.

```{r}
fun_plot_evol_typicite = function(table_flori, site) {
  
  dt_typicite_evol = fun_evol_typicite(table_flori, site)
  
  plot_evol_typi = ggplot(
    dt_typicite_evol,
    aes(
      x = session_year,
      y = prop_habitat,
      colour = HABITAT,
      group = HABITAT
    )
  ) +
    geom_line(linewidth = 1) +
    geom_point(size = 3) +
    scale_colour_manual(values = c(
      "prairie" = "#C2CEAAFF",
      "friche"  = "#648C16FF",
      "gazon"   = "darkgreen"
    )) +
    labs(title = "proportion espèces typiques") +
    theme_minimal() +
    scale_x_continuous(limits = c(
      min(dt_typicite_evol$session_year),
      max(dt_typicite_evol$session_year)
    ),
    breaks = seq(
      min(dt_typicite_evol$session_year),
      max(dt_typicite_evol$session_year),
      by = 1
    ))
  

  return(plot_evol_typi)
  
  
}

fun_plot_evol_typicite(df_VA, 1109)
# fun_plot_evol_typicite(df_VM, 645)
fun_plot_evol_typicite(dt_ref_national, 959) #gazon
fun_plot_evol_typicite(dt_ref_national, 689)# friche
fun_plot_evol_typicite(dt_ref_national, 687) # prairie
```

## gestion

On s'intéresse aux données de fauche. Il est nécessaire de les nettoyer au préalable (application des filtres de Gabriel Sagot - stage M2 -2025). Ce script détaille les étapes de nettoyage et les modalité de calcul des scores de fauche. Le df data_ISG produit à la fin de ce script sert dans les chunks suivants. Il y a 2 scores de fauche : le score de fauche sans bonus (colonne 'score_fauche_sansbonus'), prenant les valeurs 1, 2, 4 et 6 & le score de fauche (colonne 'score_fauche'), prenant les valeurs entières allant de 1 à 8.


```{r}
source(here::here("gestion_florileges_V3.R") )
#on obtient data_ISG pour toutes les sessions florilèges
```

### Intensité moyenne de la fauche pratiquée sur le site

Ce script permet de calculer une intensité de fauche moyenne pratiquée sur le site considéré et de replacer cette valeur par rapport aux valeurs de référence. On considère ici le score de fauche sans bonus.

```{r}


fun_fauche_moyenne = function(dt_site, dt_ref) {
  
  # calcul de la valeur moyenne de gestion sur le site
 dt_site_ISG = dt_site %>%
    select(session_id, site_id) %>%
    unique() %>%
    left_join(data_ISG %>% select(session_id, score_fauche_sansbonus) %>% unique()) %>%
    na.omit() %>%
    mutate(moy_score_fauche = ceiling(mean(score_fauche_sansbonus))) %>%
    select(site_id, moy_score_fauche) %>%
    unique() 
  
 
 # récupération des valeurs d'ISG pour les sites du référentiel
  dt_ISG_ref = data_ISG %>%
    filter(session_id %in% unique(dt_ref$session_id)) %>%
    select(site_id, ISG_new, score_fauche_sansbonus) %>%
    filter(!is.na(score_fauche_sansbonus)) %>%
    distinct()
  
  DT::datatable(head(dt_ISG_ref))
  
  plot_fauche =
    ggplot() +
    geom_bar(
      data = dt_ISG_ref,
      aes(x = factor(score_fauche_sansbonus)),
      stat = "count",
      fill = "#11C2B5FF",
      width = 0.6
    ) +
    geom_point(
      data = dt_site_ISG,
      aes(x = factor(moy_score_fauche), y = 0),
      color = "#F76E5EFF",
      size = 6
    ) +
    labs(x = "score d'intensité de la fauche", y = "nombre de sites") +
    theme_minimal() +
    theme(legend.position = "none") 

  
  return(plot_fauche) 
  
}


fun_fauche_moyenne(dt_site = df_VA, dt_ref = dt_ref_VA)
fun_fauche_moyenne(dt_site = df_VM, dt_ref = dt_ref_VM)




```

### Evolution de l'intensité de la fauche

visualisation de l'évolution de l'intensité de la fauche grâce au score de fauche au cours des années de suivi.

```{r}

fun_evol_fauche = function(dt_site) {
  
  dt_site_ISG = dt_site %>%
    select(session_id, site_id, session_year) %>%
    unique() %>%
    left_join(data_ISG %>% select(session_id, session_year, score_fauche) %>% unique()) %>%
    unique()
  
  DT::datatable(head(dt_site_ISG))
  
  plot_evol_fauche =
    ggplot() +
    geom_point(
      data = dt_site_ISG,
      aes(x = session_year, y = score_fauche),
      color = "#F76E5EFF",
      size = 6
    ) +
    labs(y = "score d'intensité de la fauche", x = "années") +
    theme_minimal() +
    theme(legend.position = "none") +
    scale_x_continuous(limits =
                         c(
                           min(dt_site_ISG$session_year),
                           max(dt_site_ISG$session_year)
                         ),
                       breaks = seq(
                         min(dt_site_ISG$session_year),
                         max(dt_site_ISG$session_year) ,
                         by = 1
                       )) +
    scale_y_continuous(limits = c(0, 8), breaks = seq(0, 8, by = 1))
  
  return(plot_evol_fauche)
}


fun_evol_fauche(dt_site = df_VA)


# fun_evol_fauche(dt_site = df_VM)


```
